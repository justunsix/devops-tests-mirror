# Make Git normalize .txt files, 
# ensure .sh files have LF in the working directory
# and prevent .png files from being normalized regardless of their content
*           text=auto
*.txt		text
*.sh		text eol=lf
*.jpg		-text